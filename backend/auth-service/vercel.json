{
  "version": 2,
  "name": "learnflow-auth",
  "installCommand": "rm -rf node_modules && npm install",
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js"
    }
  ]
}
